import { CustomerPipe1Pipe } from './customer-pipe-1.pipe';

describe('CustomerPipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerPipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
